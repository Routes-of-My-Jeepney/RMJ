import React from "react";
import MapContainer from "../components/MapContainer";
import { Box } from "@mui/material";

const Home = () => {
    return (
        <Box>
            <MapContainer />
        </Box>
    );
};

export default Home;
